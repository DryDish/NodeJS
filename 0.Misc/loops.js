// Funtional looping methods are preffered
// such as map, reduce, filter, find ect.

const fruits = ["apples", "bananas", "coconut"]

// use map to print out each fruit
fruits.map(fruit => console.log({fruit: fruit}));

// create a new server with express called data_server 